server {
    listen 80;
    root /opt/app/dist;

    location / {
        try_files $uri $uri/ /user/info.html;
    }

    location /api/ {
        proxy_pass https://2023-im-backend-killthisse.app.secoder.net/;
    }
}
